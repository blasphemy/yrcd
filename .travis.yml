language: c
compiler: gcc
before_install:
  - sudo add-apt-repository ppa:vala-team/ppa -y
  - sudo apt-get update -qq
  - sudo apt-get install -qq gnome-common libglib2.0-dev libvala-0.24-dev valac-0.24 gobject-introspection libgee-dev
script:
  - cmake .
  - make
notifications:
  irc: "sterling.notroll.me#yrcd"
